# ecommerce-cro-auditor

סקיל ל-Claude שמבצע ביקורת CRO (אופטימיזציית שיעור המרה) מלאה ושיטתית לחנות אונליין, בעברית.

הסקיל מכסה בתהליך אחד את מה שבדרך כלל מפוזר בין כמה כלים: CRO לאיקומרס, UI/UX, פסיכולוגיה צרכנית, מובייל, נגישות וביצועים, ביקורת רב-סוכנית, ומעבר שיטתי על כל האתר. התוצר הוא רשימת שינויים מדויקת לפי אזור באתר, מדורגת לפי השפעה, ביטחון ומאמץ, ומסווגת לעובדה מבוססת, השערה או ניסוי A/B. יישום בקוד מתבצע רק אחרי אישור מפורש.

## התהליך

| שלב | מה קורה | תוצר |
|---|---|---|
| 0 | תדריך עם בעל החנות, וסריקה אוטומטית: גילוי עמודים, צילומי מסך במובייל ובדסקטופ, אותות טכניים | `01-brief.md`, `02-crawl/` |
| 1 | מחקר עומק ממקורות מקצועיים (Baymard, Nielsen Norman Group, WCAG, web.dev, בנצ'מרקים לענף) | `03-research.md` |
| 2 | חמישה מסמכי ידע מותאמים לחנות: CRO, UX, פסיכולוגיה, מובייל, כלים | `04-knowledge/` |
| 3 | סקירה של שישה סוכני AI, כל אחד בתפקיד אחר | `05-agents/*.json` |
| 4 | איחוד הממצאים, זיהוי כפילויות, ערעורים, בדיקת סתירות והכרעה | `06-merged/` |
| 5 | סריקה שיטתית עמוד-עמוד, במובייל ובדסקטופ, אימות כל ממצא | `07-page-scan.md` |
| 6 | רשימת שינויים מדויקת לפי אזור באתר | `08-final-report.md` |
| 7 | דירוג כל שינוי: השפעה × ביטחון × קלות | (באותו דוח) |
| 8 | סיווג: עובדה מבוססת, השערה, או ניסוי A/B, עם כרטיס ניסוי לכל ניסוי | `09-ab-tests.md` |
| 9 | רק אחרי אישור: יישום בקוד, עם תיעוד ומדידה | `10-implementation.md` |

## ששת הסוכנים

שישה סוכנים שמקבלים אותה הנחיה מחזירים שש גרסאות של אותה רשימה. לכן לכל סוכן יש תפקיד, זווית מבט, רשימת בדיקה משלו, ורשימה מפורשת של מה *לא* לבדוק:

| סוכן | מה הוא מכסה |
|---|---|
| **מומחה CRO** | המשפך: הצעת ערך, קריאות לפעולה, מחיר ומשלוח, עגלה, צ'קאאוט, אמון |
| **חוקר UX** | ניווט, חיפוש, פילטרים, טפסים, משוב, שגיאות, עקביות, RTL |
| **פסיכולוג צרכני** | הוכחה חברתית, מחסור, עיגון, עומס בחירה, ניסוח, דפוסים אפלים |
| **מומחה מובייל** | אזור האגודל, מקלדת, גלילה, CTA דביק, תשלום מהיר, ביצועים במובייל |
| **מומחה נגישות וביצועים** | WCAG 2.2, ניגודיות, מקלדת, Core Web Vitals, משקל עמוד |
| **Devil's Advocate** | לא מחפש בעיות באתר, אלא חורים בממצאים של החמישה: ראיות חלשות, סיכונים, סתירות, נקודות עיוורות |

## התקנה

### Claude Code, דרך marketplace (הכי פשוט)

```
/plugin marketplace add galsolomon/2-3-game
/plugin install ecommerce-cro-auditor@galsolomon-skills
```

### Claude Code, העתקה ידנית

```bash
git clone https://github.com/galsolomon/2-3-game.git
mkdir -p ~/.claude/skills
cp -r 2-3-game/skills/ecommerce-cro-auditor ~/.claude/skills/
```

לשימוש בפרויקט אחד בלבד, להעתיק ל-`.claude/skills/` בתוך הפרויקט במקום לתיקיית הבית.

### Claude.ai (האפליקציה או האתר)

להוריד את `dist/ecommerce-cro-auditor.skill` ולהעלות אותו בהגדרות, תחת Skills (השם המדויק של התפריט משתנה בין גרסאות: "Capabilities" או "Customize"). ב-Claude.ai אין סוכנים מקבילים, ולכן ששת התפקידים רצים בזה אחר זה; הסקיל יודע את זה ומציין זאת בדוח.

### דרישות

- Python 3.8 ומעלה (הסקריפטים משתמשים רק בספרייה הסטנדרטית).
- לצילומי מסך ולמדדי פריסה: Playwright. `pip install playwright` ואז `playwright install chromium`. בלי Playwright הסקיל עובד במצב `--no-browser`, בלי צילומי מסך.

## שימוש

לכתוב ל-Claude משהו כמו:

> תעשה ביקורת CRO מלאה לחנות שלי: https://example.co.il. זו חנות Shopify לבגדי ילדים, רוב התנועה מהמובייל, שיעור ההמרה בערך 1.2%.

או פשוט:

> למה אנשים לא קונים באתר שלי? https://example.co.il

Claude יטען את הסקיל, יפתח תיקיית `cro-audit/`, ויעבור על תשעת השלבים. בסוף שלב 8 הוא עוצר, מציג את תקציר המנהלים ואת עשרת השינויים הראשונים, ושואל אילו מהם לבצע. הוא לא נוגע בקוד לפני תשובה.

אפשר גם להפעיל חלקים בנפרד:

```bash
# סריקה בלבד
python ~/.claude/skills/ecommerce-cro-auditor/scripts/crawl_site.py https://example.co.il --out cro-audit/02-crawl --try-checkout

# איחוד ממצאים של סוכנים שכבר רצו
python ~/.claude/skills/ecommerce-cro-auditor/scripts/merge_findings.py cro-audit/05-agents --out cro-audit/06-merged

# בניית שלד הדוח
python ~/.claude/skills/ecommerce-cro-auditor/scripts/build_report.py --audit cro-audit
```

## מבנה הסקיל

```
skills/ecommerce-cro-auditor/
├── SKILL.md                      התהליך המלא, תשעת השלבים, כללי הברזל
├── agents/                       הנחיות תפקיד לששת הסוכנים
├── references/                   מסמכי ידע: CRO, UX, פסיכולוגיה, מובייל, נגישות וביצועים,
│                                 כלים, שוק ישראלי, מקורות מחקר, פרוטוקול סריקה, איחוד, דירוג
├── assets/                       סכימת הממצאים (JSON) ותבניות: תדריך, דוח סופי, כרטיס A/B
└── scripts/
    ├── init_audit.py             פותח תיקיית ביקורת
    ├── crawl_site.py             סריקה, צילומי מסך, אותות טכניים
    ├── merge_findings.py         איחוד, כפילויות, ערעורים, הכרעות, דירוג
    └── build_report.py           שלד הדוח וכרטיסי A/B
```

## עקרונות

1. **תפקידים שונים, לא שש דעות דומות.** לכל סוכן יש מה לבדוק ומה לא, וה-Devil's Advocate בודק את הסוכנים ולא את האתר.
2. **תצפית ומקור לפני דעה.** ממצא בלי צילום מסך או מדד הוא ניחוש; ממצא בלי מקור הוא טעם אישי. שניהם נכנסים לדוח רק כהשערה.
3. **המלצה לפני יישום.** הסקיל מסתיים ברשימה מאושרת, לא בקוד. שינוי בחנות פעילה בלי אישור הוא סיכון עסקי.
4. **בלי דפוסים אפלים.** טיימרים מזויפים, מלאי מזויף, הרשמה מסומנת מראש: הסקיל מסמן אותם כממצא שלילי, לא כהמלצה.
5. **הכל בעברית ברורה.** מונחים שמקובלים באנגלית (CRO, CTA, Checkout, A/B) נשארים באנגלית עם הסבר בפעם הראשונה.

## שאלות נפוצות

**הסקיל משנה את האתר שלי?** לא, עד שמאשרים במפורש אילו פריטים לבצע (שלב 9). כל השלבים הקודמים קוראים בלבד.

**כמה זמן זה לוקח?** תלוי בגודל החנות ובסביבה. ב-Claude Code עם סוכנים מקבילים, ביקורת מלאה של 25 עמודים אורכת בדרך כלל בין שעה לשלוש שעות של עבודת Claude. ב-Claude.ai בלי סוכנים מקבילים, יותר.

**זה עובד על חנות שלא בעברית?** כן. התוצרים בעברית, ציטוטים מהאתר בשפת המקור. מסמך השוק הישראלי נטען רק אם החנות ישראלית.

**זה מחליף ניסויי A/B?** לא. הסקיל מפריד בין מה שידוע (עובדה), מה שסביר (השערה) ומה שצריך לבדוק (ניסוי), ובונה כרטיס ניסוי לכל פריט מהסוג השלישי. הוא גם אומר בכנות מתי לחנות אין מספיק תנועה לניסוי.

**המספרים במסמכי הידע מדויקים?** הם נקודת פתיחה ממחקרים ידועים (Baymard, Nielsen Norman Group, W3C, web.dev), ושלב 1 בתהליך נועד לאמת אותם מחדש בכל ביקורת. אם אין גישה לאינטרנט, הסקיל כותב זאת במפורש.

## רישיון

MIT. אפשר להשתמש, לשנות ולשתף. ראה `LICENSE`.
